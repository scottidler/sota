
main = ()

    list1 = [1, 3, 4, 5, 7]
    list2 = [1, 3-5, 7]

    if list1 == list2
        print 'yep they are the same'
        print 'dashes create range expressions between integers instead of subtraction'

    # error, the dash makes 5 negative, but does not subtract it from 3
    list3 = [1, 3 -5, 7]

    list4 = [1, 3 - 5, 7]
    list4 = [1, -2, 7]

    if list3 == list4
        print 'yep they are the same'
        print 'this is subtraction rather than a range expression' 
