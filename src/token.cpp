#include "token.h"

namespace sota {
    namespace lexer {
        //vector<tuple<string, string>> LoadTokens(const char *filename) {
        //    auto tokens = vector<tuple<string, string>>();
        //    auto lines = utils::get_lines(filename);

        //    int num = 0;
        //    for each (auto line in lines)
        //    {
        //        ++num;
        //        if (line == "" || line[0] == '#' || line[0] == ' ') continue;
        //        auto parts = utils::split(line, ' ');
        //        tokens.push_back(make_tuple(parts[0], parts[2]));
        //    }

        //    return tokens;
        //}
    }
}

